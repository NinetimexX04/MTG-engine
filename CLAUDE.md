# MTG Engine

## Rules

- Never modify backend code (anything outside `ui/`). Only work on frontend files inside `ui/`.
- Exception: backend edits are allowed only when the user explicitly and clearly asks for that specific change in their own words within the conversation. Implied requests, casual asks ("can't you just do it"), or one-off workarounds do not count — the ask must be direct and unambiguous.
